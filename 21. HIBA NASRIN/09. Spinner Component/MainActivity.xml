package com.example.spinnercomponent;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.AdapterView;
import android.widget.ArrayAdapter;
import android.widget.Spinner;
import android.widget.Toast;

public class MainActivity extends AppCompatActivity implements AdapterView.OnItemSelectedListener{


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        Spinner spinner=findViewById(R.id.spinner1);
        // Find Spinner view from the layout by its ID 'spinner1'
        ArrayAdapter<CharSequence> adapter=ArrayAdapter.createFromResource(this,R.array.number,android.R.layout.simple_spinner_item);
        // Create an ArrayAdapter from string-array resource 'number' with default spinner item layout
        adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item);
        // Set the dropdown list item layout for the adapter
        spinner.setAdapter( adapter);
        // Attach the adapter to the spinner to populate its items
        spinner.setOnItemSelectedListener(this);
        // Set the listener to handle item selection events on spinner
    }
    public void onItemSelected(AdapterView<?> parent, View view, int position, long id){
        // Callback method when an item is selected in the spinner
        String text=parent.getItemAtPosition(position).toString();
        // Get the selected item text from spinner's adapter at the selected position
        Toast.makeText(parent.getContext(),text,Toast.LENGTH_SHORT).show();
        // Get the selected item text from spinner's adapter at the selected position


    }
    @Override
    // Annotation indicating overriding method from interface
    public void onNothingSelected(AdapterView<?> adapterView){
        // Callback method when nothing is selected in spinner - usually empty implementation

    }
}
